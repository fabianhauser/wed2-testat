<main>
    <header>
        <div class="toolbar">
            <a href="./new" class="button">Create new Note</a>
            <a href="./?styleswitch=true" class="button">Style Switcher</a>
        </div>
        <div class="toolbar filter">
            <a href="./?orderBy=dueDate" class="button">By finished Date</a>
            <a href="./?orderBy=createdAt" class="button">By created Date</a>
            <a href="./?orderBy=rating" class="button">By Importance</a>
	    {{#if config.notes.filterBy}}
              <a href="./?filterBy=finished" class="button">Fertige verbergen</a>
            {{else}}
              <a href="./?filterBy=" class="button">Fertige verbergen</a>
            {{/if}}
        </div>
    </header>
    <section class="content">
        {{#if notes}}
            {{#each notes}}
                <article>
                    <header>
                        <span>{{dueDate}}</span>
                        <span>{{title}}</span>
                        <span>{{rating}}</span>
                    </header>
                    <div>
                        <input type="radio" value="{{finished}}" readonly>
                        <textarea readonly></textarea>
                        <form action="/" method="POST">
                            <button type="submit">Edit</button>
                        </form>
                    </div>
                </article>
            {{/each}}
        {{else}}
            <div class="toolbox">
                <p>Keine ToDo's gefuden :(</p>
            </div>
        {{/if}}
    </section>
</main>
